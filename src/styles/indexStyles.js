import { createUseStyles } from "react-jss"

export default createUseStyles(theme => ({
    container: {
        display: "flex",
        flexDirection: 'column',
        marginBottom: "3rem",
    },
    topDiv: {
        display: "flex",
    },
}))


